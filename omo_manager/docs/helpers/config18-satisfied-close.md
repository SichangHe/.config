config:18 satisfied-child close

- purpose
  - close only the completed `config16_close.md` worker under Human Source 1570
  - consume its one exact satisfied pending item after the committed config:16 audit
- prepare
  - bind exact authority, upstream audit, task, TODO, manager, pane, process, session, tail, and full composer capture
  - snapshot config:19, config:20, and dw2:0 as protected targets
  - publish one immutable private packet
- execute
  - accept only an authenticated independent PASS for the exact packet
  - allow only the live wl:21 pane
  - publish a prepared audit before closing the bound config:18 pane without terminal input
  - move the exact TODO row to previous and remove only the bound pending item
  - recover only initial, TODO-first, or committed byte states
  - publish the committed audit after target absence, lifecycle completion, and protected snapshots are proven
